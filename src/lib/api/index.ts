/**
 * API pública da camada de transporte da simulação (ETAPA 10).
 *
 * DTOs, validação, normalização e mapeamento entre o contrato público e o
 * domínio. Não contém regra de negócio.
 */

export * from "./simulation-dto";
