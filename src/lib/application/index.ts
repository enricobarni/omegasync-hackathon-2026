/**
 * API pública da camada de aplicação/orquestração (ETAPA 9).
 *
 * Coordena os módulos determinísticos (catálogo, anuência, elegibilidade,
 * custos, comparação, janela). Não contém regra de negócio própria.
 */

export * from "./simulation-service";
